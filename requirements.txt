# Inference API dependencies (CPU-only for AWS Lambda).
# The torch CPU wheel is pulled from the PyTorch index in the Dockerfile via
# --extra-index-url https://download.pytorch.org/whl/cpu to avoid shipping CUDA.

torch==2.3.1
transformers==4.53.3
tokenizers>=0.19,<0.22
safetensors>=0.4.3
sentencepiece==0.2.0

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1
